# HTTPS потоковая передача с протоколом контекста модели (MCP)

Эта глава представляет собой комплексное руководство по реализации безопасной, масштабируемой и потоковой передачи данных в реальном времени с использованием Протокола Контекста Модели (MCP) через HTTPS. Рассматриваются мотивация для потоковой передачи, доступные транспортные механизмы, как реализовать потоковый HTTP в MCP, лучшие практики безопасности, миграция с SSE и практические рекомендации по созданию собственных потоковых приложений MCP.

> **Взгляд в будущее:** этот урок описывает Потоковый HTTP согласно **MCP Specification 2025-11-25**, где сессия устанавливается во время `initialize` и фиксируется с помощью заголовка `Mcp-Session-Id`. В релизе-кандидате `2026-07-28` рукопожатие и идентификатор сессии полностью удаляются, делая каждый запрос автономным и маршрутизируемым на любой сервер без необходимости удерживать сессии. Подробнее см. в [Что меняется в MCP: релиз-кандидат 2026-07-28](../../01-CoreConcepts/mcp-2026-07-28-release-candidate.md).

## Транспортные механизмы и потоковая передача в MCP

В этом разделе рассматриваются различные транспортные механизмы, доступные в MCP, и их роль в обеспечении возможностей потоковой передачи для общения в реальном времени между клиентами и серверами.

### Что такое транспортный механизм?

Транспортный механизм определяет, как данные обмениваются между клиентом и сервером. MCP поддерживает несколько типов транспорта, чтобы соответствовать разным средам и требованиям:

- **stdio**: Стандартный ввод/вывод, подходит для локальных и CLI-инструментов. Простой, но не подходит для веба или облака.
- **SSE (Server-Sent Events)**: Позволяет серверам отправлять обновления клиентам в реальном времени по HTTP. Хорош для веб-интерфейсов, но ограничен в масштабируемости и гибкости. Начиная с MCP Specification 2025-06-18, независимый транспорт SSE признан устаревшим и заменён транспортом "Streamable HTTP".
- **Streamable HTTP**: Современный потоковый транспорт на основе HTTP, поддерживающий уведомления и лучшую масштабируемость. Рекомендуется для большинства производственных и облачных сценариев.

### Таблица сравнения

Посмотрите таблицу сравнения ниже, чтобы понять различия между этими транспортными механизмами:

| Транспорт         | Обновления в реальном времени | Потоковая передача | Масштабируемость | Сценарий использования         |
|-------------------|------------------------------|--------------------|-----------------|------------------------------|
| stdio             | Нет                          | Нет                | Низкая          | Локальные CLI-инструменты    |
| SSE               | Да                           | Да                 | Средняя         | Веб, обновления в реальном времени |
| Streamable HTTP   | Да                           | Да                 | Высокая         | Облако, многоклиентские среды |

> **Совет:** Выбор правильного транспорта влияет на производительность, масштабируемость и опыт пользователя. **Streamable HTTP** рекомендуется для современных, масштабируемых и готовых к облаку приложений.

Обратите внимание на транспорты stdio и SSE, которые были показаны в предыдущих главах, и как потоковый HTTP является транспортом, рассматриваемым в этой главе.

## Потоковая передача: концепции и мотивация

Понимание основных концепций и мотиваций, лежащих в основе потоковой передачи, важно для реализации эффективных систем коммуникации в реальном времени.

**Потоковая передача** — это техника в сетевом программировании, позволяющая отправлять и получать данные малыми, управляемыми частями или в виде последовательности событий, вместо ожидания полной готовности всего отклика. Это особенно полезно для:

- Больших файлов или наборов данных.
- Обновлений в реальном времени (например, чат, индикаторы прогресса).
- Длительных вычислений, когда нужно информировать пользователя по ходу выполнения.

Вот основные моменты потоковой передачи:

- Данные доставляются поэтапно, а не все сразу.
- Клиент может обрабатывать данные по мере их поступления.
- Уменьшается воспринимаемая задержка и улучшается опыт пользователя.

### Зачем использовать потоковую передачу?

Причины использовать потоковую передачу следующие:

- Пользователи получают обратную связь сразу, а не только в конце.
- Обеспечивает поддержку приложений в реальном времени и отзывчивого интерфейса.
- Более эффективное использование сетевых и вычислительных ресурсов.

### Простой пример: HTTP потоковый сервер и клиент

Вот простой пример реализации потоковой передачи:

#### Python

**Сервер (Python, с использованием FastAPI и StreamingResponse):**

```python
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
import time

app = FastAPI()

async def event_stream():
    for i in range(1, 6):
        yield f"data: Message {i}\n\n"
        time.sleep(1)

@app.get("/stream")
def stream():
    return StreamingResponse(event_stream(), media_type="text/event-stream")
```

**Клиент (Python, с использованием requests):**

```python
import requests

with requests.get("http://localhost:8000/stream", stream=True) as r:
    for line in r.iter_lines():
        if line:
            print(line.decode())
```

Этот пример демонстрирует, как сервер отправляет клиенту серию сообщений по мере их готовности, вместо того, чтобы ждать готовности всех сообщений сразу.

**Как это работает:**

- Сервер передаёт каждое сообщение по мере его готовности.
- Клиент получает и выводит каждую часть по мере их поступления.

**Требования:**

- Сервер должен использовать потоковый ответ (например, `StreamingResponse` в FastAPI).
- Клиент должен обрабатывать ответ как поток (`stream=True` в requests).
- Content-Type обычно `text/event-stream` или `application/octet-stream`.

#### Java

**Сервер (Java, с использованием Spring Boot и Server-Sent Events):**

```java
@RestController
public class CalculatorController {

    @GetMapping(value = "/calculate", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
    public Flux<ServerSentEvent<String>> calculate(@RequestParam double a,
                                                   @RequestParam double b,
                                                   @RequestParam String op) {
        
        double result;
        switch (op) {
            case "add": result = a + b; break;
            case "sub": result = a - b; break;
            case "mul": result = a * b; break;
            case "div": result = b != 0 ? a / b : Double.NaN; break;
            default: result = Double.NaN;
        }

        return Flux.<ServerSentEvent<String>>just(
                    ServerSentEvent.<String>builder()
                        .event("info")
                        .data("Calculating: " + a + " " + op + " " + b)
                        .build(),
                    ServerSentEvent.<String>builder()
                        .event("result")
                        .data(String.valueOf(result))
                        .build()
                )
                .delayElements(Duration.ofSeconds(1));
    }
}
```

**Клиент (Java, с использованием Spring WebFlux WebClient):**

```java
@SpringBootApplication
public class CalculatorClientApplication implements CommandLineRunner {

    private final WebClient client = WebClient.builder()
            .baseUrl("http://localhost:8080")
            .build();

    @Override
    public void run(String... args) {
        client.get()
                .uri(uriBuilder -> uriBuilder
                        .path("/calculate")
                        .queryParam("a", 7)
                        .queryParam("b", 5)
                        .queryParam("op", "mul")
                        .build())
                .accept(MediaType.TEXT_EVENT_STREAM)
                .retrieve()
                .bodyToFlux(String.class)
                .doOnNext(System.out::println)
                .blockLast();
    }
}
```

**Примечания по реализации на Java:**

- Использует реактивный стек Spring Boot с `Flux` для потоковой передачи
- `ServerSentEvent` обеспечивает структурированную потоковую передачу событий с типами событий
- `WebClient` с `bodyToFlux()` позволяет потреблять потоковые данные реактивно
- `delayElements()` имитирует время обработки между событиями
- События могут иметь типы (`info`, `result`) для лучшей обработки клиентом

### Сравнение: классическая потоковая передача vs потоковая передача в MCP

Различия между классическим подходом к потоковой передаче и тем, как это реализовано в MCP, можно показать так:

| Особенность            | Классический HTTP Стриминг     | MCP Стриминг (Уведомления)         |
|-----------------------|-------------------------------|------------------------------------|
| Основной ответ         | По частям (chunked)            | Один, в конце                      |
| Обновления прогресса   | Отправляются как части данных  | Отправляются как уведомления       |
| Требования к клиенту   | Должен обрабатывать поток      | Должен реализовать обработчик сообщений |
| Сценарий использования | Большие файлы, потоки токенов AI | Прогресс, логи, обратная связь в реальном времени |

### Основные отличия

Кроме того, есть некоторые ключевые отличия:

- **Паттерн общения:**
  - Классический HTTP стриминг: Использует простое кодирование передачи частями (chunked)
  - MCP стриминг: Использует структурированную систему уведомлений с протоколом JSON-RPC

- **Формат сообщения:**
  - Классический HTTP: Простые текстовые блоки с переносами строк
  - MCP: Структурированные объекты LoggingMessageNotification с метаданными

- **Реализация клиента:**
  - Классический HTTP: Простой клиент, который обрабатывает потоковые ответы
  - MCP: Более сложный клиент с обработчиком сообщений для обработки разных типов сообщений

- **Обновления прогресса:**
  - Классический HTTP: Прогресс является частью основного потока ответов
  - MCP: Прогресс отправляется отдельными сообщениями-уведомлениями, а основной ответ приходит в конце

### Рекомендации

Вот что мы рекомендуем при выборе между классической потоковой передачей (как в эндпоинте `/stream`, показанном выше) и потоковой передачей через MCP.

- **Для простых задач потоковой передачи:** Классический HTTP стриминг проще реализуется и подходит для базовых потребностей.

- **Для сложных интерактивных приложений:** MCP стриминг обеспечивает более структурированный подход с богатыми метаданными и разделением уведомлений и окончательных результатов.

- **Для AI-приложений:** Система уведомлений MCP особенно полезна для длительных AI-задач, где нужно держать пользователей в курсе прогресса.

## Потоковая передача в MCP

Итак, вы уже видели рекомендации и сравнения между классической потоковой передачей и MCP-стримингом. Теперь давайте подробно разберем, как именно можно использовать потоковую передачу в MCP.

Понимание работы потоковой передачи в рамках MCP важно для создания отзывчивых приложений, которые обеспечивают обратную связь в реальном времени во время длительных операций.

В MCP потоковая передача не о передаче основного ответа частями, а о отправке **уведомлений** клиенту во время обработки запроса. Эти уведомления могут включать обновления прогресса, логи или другие события.

### Как это работает

Основной результат всё ещё отправляется как один ответ. Однако уведомления могут приходить отдельными сообщениями во время обработки, обновляя клиент в реальном времени. Клиент должен уметь обрабатывать и отображать эти уведомления.

## Что такое уведомление?

Мы говорим "уведомление" — что это значит в контексте MCP?

Уведомление — это сообщение от сервера клиенту, информирующее о прогрессе, статусе или других событиях во время длительной операции. Уведомления улучшают прозрачность и пользовательский опыт.

Например, клиент должен отправить уведомление сразу после установления начального соединения с сервером.

Уведомление выглядит так в формате JSON:

```json
{
  jsonrpc: "2.0";
  method: string;
  params?: {
    [key: string]: unknown;
  };
}
```

Уведомления относятся к теме в MCP под названием ["Logging"](https://modelcontextprotocol.io/specification/draft/server/utilities/logging).

Чтобы логирование работало, сервер должен включить эту функцию/возможность следующим образом:

```json
{
  "capabilities": {
    "logging": {}
  }
}
```

> [!NOTE]
> В зависимости от используемого SDK, логирование может быть включено по умолчанию, или его нужно явно активировать в конфигурации сервера.

Существуют разные типы уведомлений:

| Уровень      | Описание                       | Пример использования             |
|-------------|-------------------------------|---------------------------------|
| debug       | Детальная отладочная информация | Точки входа/выхода функций       |
| info        | Общие информационные сообщения | Обновления о прогрессе операции  |
| notice      | Обычные, но значимые события   | Изменения конфигурации           |
| warning     | Предупредительные условия      | Использование устаревших функций |
| error       | Ошибочные условия              | Сбой операций                   |
| critical    | Критические условия            | Ошибки компонентов системы        |
| alert       | Требуется немедленное действие | Обнаружено повреждение данных    |
| emergency   | Система недоступна             | Полный отказ системы             |

## Реализация уведомлений в MCP

Чтобы реализовать уведомления в MCP, нужно настроить как серверную, так и клиентскую стороны для обработки обновлений в реальном времени. Это позволяет вашему приложению предоставлять мгновенную обратную связь пользователям во время длительных операций.

### На стороне сервера: отправка уведомлений

Начнём с серверной стороны. В MCP вы определяете инструменты, которые могут отправлять уведомления во время обработки запросов. Сервер использует объект контекста (обычно `ctx`) для отправки сообщений клиенту.

#### Python

```python
@mcp.tool(description="A tool that sends progress notifications")
async def process_files(message: str, ctx: Context) -> TextContent:
    await ctx.info("Processing file 1/3...")
    await ctx.info("Processing file 2/3...")
    await ctx.info("Processing file 3/3...")
    return TextContent(type="text", text=f"Done: {message}")
```

В приведённом примере инструмент `process_files` отправляет три уведомления клиенту по мере обработки каждого файла. Метод `ctx.info()` используется для отправки информационных сообщений.

Кроме того, чтобы включить уведомления, убедитесь, что ваш сервер использует потоковый транспорт (например, `streamable-http`), а клиент реализует обработчик сообщений для обработки уведомлений. Вот как настроить сервер для использования транспорта `streamable-http`:

```python
mcp.run(transport="streamable-http")
```

#### .NET

```csharp
[Tool("A tool that sends progress notifications")]
public async Task<TextContent> ProcessFiles(string message, ToolContext ctx)
{
    await ctx.Info("Processing file 1/3...");
    await ctx.Info("Processing file 2/3...");
    await ctx.Info("Processing file 3/3...");
    return new TextContent
    {
        Type = "text",
        Text = $"Done: {message}"
    };
}
```

В этом примере на .NET инструмент `ProcessFiles` украшен атрибутом `Tool` и отправляет три уведомления клиенту по мере обработки каждого файла. Метод `ctx.Info()` используется для отправки информационных сообщений.

Чтобы включить уведомления в вашем MCP сервере на .NET, убедитесь, что вы используете потоковый транспорт:

```csharp
var builder = McpBuilder.Create();
await builder
    .UseStreamableHttp() // Enable streamable HTTP transport
    .Build()
    .RunAsync();
```

### На стороне клиента: получение уведомлений

Клиент должен реализовать обработчик сообщений для обработки и отображения уведомлений по мере их поступления.

#### Python

```python
async def message_handler(message):
    if isinstance(message, types.ServerNotification):
        print("NOTIFICATION:", message)
    else:
        print("SERVER MESSAGE:", message)

async with ClientSession(
   read_stream, 
   write_stream,
   logging_callback=logging_collector,
   message_handler=message_handler,
) as session:
```

В приведённом коде функция `message_handler` проверяет, является ли входящее сообщение уведомлением. Если да, то оно выводится; иначе сообщение обрабатывается как обычное серверное сообщение. Обратите внимание, что `ClientSession` инициализируется с `message_handler` для обработки входящих уведомлений.

#### .NET

```csharp
// Define a message handler
void MessageHandler(IJsonRpcMessage message)
{
    if (message is ServerNotification notification)
    {
        Console.WriteLine($"NOTIFICATION: {notification}");
    }
    else
    {
        Console.WriteLine($"SERVER MESSAGE: {message}");
    }
}

// Create and use a client session with the message handler
var clientOptions = new ClientSessionOptions
{
    MessageHandler = MessageHandler,
    LoggingCallback = (level, message) => Console.WriteLine($"[{level}] {message}")
};

using var client = new ClientSession(readStream, writeStream, clientOptions);
await client.InitializeAsync();

// Now the client will process notifications through the MessageHandler
```

В этом примере на .NET функция `MessageHandler` проверяет, является ли входящее сообщение уведомлением. Если да, уведомление выводится; иначе сообщение обрабатывается как обычное серверное сообщение. `ClientSession` инициализируется с обработчиком сообщений через `ClientSessionOptions`.

Чтобы включить уведомления, убедитесь, что сервер использует потоковый транспорт (например, `streamable-http`), а клиент реализует обработчик сообщений для обработки уведомлений.

## Уведомления о прогрессе и сценарии использования

В этом разделе объясняется концепция уведомлений о прогрессе в MCP, почему они важны и как их реализовать с помощью Streamable HTTP. Также представлено практическое задание для закрепления материала.

Уведомления о прогрессе — это сообщения в реальном времени, отправляемые сервером клиенту во время длительных операций. Вместо ожидания полного завершения процесса сервер постоянно информирует клиента о текущем статусе. Это улучшает прозрачность, пользовательский опыт и облегчает отладку.

**Пример:**

```text

"Processing document 1/10"
"Processing document 2/10"
...
"Processing complete!"

```

### Зачем использовать уведомления о прогрессе?

Уведомления о прогрессе важны по нескольким причинам:

- **Лучший пользовательский опыт:** Пользователи видят обновления по мере выполнения работы, а не только в конце.
- **Обратная связь в реальном времени:** Клиенты могут показывать индикаторы прогресса или логи, делая приложение отзывчивым.
- **Проще отлагать и мониторить:** Разработчики и пользователи видят, где процесс может тормозить или зависать.

### Как реализовать уведомления о прогрессе

Вот как можно реализовать уведомления о прогрессе в MCP:

- **На сервере:** Используйте `ctx.info()` или `ctx.log()` для отправки уведомлений по мере обработки каждого элемента. Это отправляет сообщение клиенту до готовности основного результата.
- **На клиенте:** Реализуйте обработчик сообщений, который слушает и отображает уведомления по мере их поступления. Этот обработчик различает уведомления и итоговый результат.

**Пример сервера:**

#### Python

```python
@mcp.tool(description="A tool that sends progress notifications")
async def process_files(message: str, ctx: Context) -> TextContent:
    for i in range(1, 11):
        await ctx.info(f"Processing document {i}/10")
    await ctx.info("Processing complete!")
    return TextContent(type="text", text=f"Done: {message}")
```

**Пример клиента:**

#### Python

```python
async def message_handler(message):
    if isinstance(message, types.ServerNotification):
        print("NOTIFICATION:", message)
    else:
        print("SERVER MESSAGE:", message)
```

## Вопросы безопасности

При реализации MCP серверов с HTTP-транспортами безопасность становится главным вопросом, требующим внимательного рассмотрения множества векторов атак и механизмов защиты.

### Обзор

Безопасность критична при открытии MCP серверов через HTTP. Потоковый HTTP вводит новые уязвимости и требует тщательной настройки.

### Основные моменты


- **Проверка заголовка Origin**: Всегда проверяйте заголовок `Origin`, чтобы предотвратить атаки DNS rebinding.
- **Привязка к localhost**: Для локальной разработки привязывайте серверы к `localhost`, чтобы избежать их экспозиции в общедоступной сети.
- **Аутентификация**: Реализуйте аутентификацию (например, API-ключи, OAuth) для рабочих развертываний.
- **CORS**: Настройте политики Cross-Origin Resource Sharing (CORS) для ограничения доступа.
- **HTTPS**: Используйте HTTPS в продакшене для шифрования трафика.

### Лучшие практики

- Никогда не доверяйте входящим запросам без проверки.
- Логируйте и контролируйте весь доступ и ошибки.
- Регулярно обновляйте зависимости для устранения уязвимостей безопасности.

### Задачи

- Балансировка безопасности и удобства разработки
- Обеспечение совместимости с различными клиентскими окружениями

## Обновление с SSE на Streamable HTTP

Для приложений, использующих Server-Sent Events (SSE), переход на Streamable HTTP обеспечивает расширенные возможности и лучшую долговременную устойчивость ваших MCP-решений.

### Почему стоит обновиться?

Существует две убедительные причины перейти с SSE на Streamable HTTP:

- Streamable HTTP обеспечивает лучшую масштабируемость, совместимость и более расширенную поддержку уведомлений, чем SSE.
- Это рекомендуемый транспорт для новых MCP-приложений.

### Шаги миграции

Вот как вы можете перейти с SSE на Streamable HTTP в ваших MCP-приложениях:

- **Обновите код сервера** для использования `transport="streamable-http"` в `mcp.run()`.
- **Обновите код клиента** для использования `streamablehttp_client` вместо SSE клиента.
- **Реализуйте обработчик сообщений** на клиенте для обработки уведомлений.
- **Проведите тестирование на совместимость** с существующими инструментами и рабочими процессами.

### Поддержание совместимости

Рекомендуется поддерживать совместимость с существующими SSE-клиентами в процессе миграции. Вот некоторые стратегии:

- Можно поддерживать и SSE, и Streamable HTTP, запуская оба транспорта на разных конечных точках.
- Постепенно переводите клиентов на новый транспорт.

### Задачи

Обязателен учет следующих вызовов при миграции:

- Обеспечение обновления всех клиентов
- Обработка различий в доставке уведомлений

## Соображения по безопасности

Безопасность должна быть главным приоритетом при реализации любого сервера, особенно при использовании HTTP-транспортов вроде Streamable HTTP в MCP.

При реализации MCP-серверов с HTTP-транспортами безопасность становится критически важной, требующей внимательного рассмотрения различных векторов атак и защитных механизмов.

### Обзор

Безопасность очень важна при экспонировании MCP-серверов через HTTP. Streamable HTTP вводит новые поверхности для атак и требует тщательной настройки.

Вот ключевые соображения по безопасности:

- **Проверка заголовка Origin**: Всегда проверяйте заголовок `Origin`, чтобы избежать атак DNS rebinding.
- **Привязка к localhost**: Для локальной разработки привязывайте серверы к `localhost`, чтобы избежать их открытия для публичного интернета.
- **Аутентификация**: Реализуйте аутентификацию (например, API-ключи, OAuth) для продакшен-развертываний.
- **CORS**: Настройте политики Cross-Origin Resource Sharing (CORS) для ограничения доступа.
- **HTTPS**: Используйте HTTPS в продакшене для шифрования трафика.

### Лучшие практики

Также вот некоторые лучшие практики по обеспечению безопасности вашего MCP streaming-сервера:

- Никогда не доверяйте входящим запросам без проверки.
- Логируйте и мониторьте всю активность и ошибки.
- Регулярно обновляйте зависимости, чтобы устранять уязвимости.

### Задачи

Вы столкнетесь с рядом задач при обеспечении безопасности MCP streaming-серверов:

- Балансировка безопасности и удобства разработки
- Обеспечение совместимости с разными клиентскими окружениями

### Задание: Создайте собственное потоковое MCP-приложение

**Сценарий:**
Постройте MCP сервер и клиент, где сервер обрабатывает список элементов (например, файлов или документов) и отправляет уведомление для каждого обработанного элемента. Клиент должен отображать каждое уведомление по мере его поступления.

**Шаги:**

1. Реализуйте серверный инструмент, который обрабатывает список и отправляет уведомления для каждого элемента.
2. Реализуйте клиент с обработчиком сообщений для отображения уведомлений в реальном времени.
3. Проверьте работу, запустив сервер и клиент, и наблюдайте уведомления.

[Решение](./solution/README.md)

## Дополнительные материалы и что дальше?

Чтобы продолжить изучение потокового MCP и расширить свои знания, в этом разделе представлены дополнительные ресурсы и предложены следующие шаги для создания более сложных приложений.

### Дополнительное чтение

- [Microsoft: Введение в HTTP Streaming](https://learn.microsoft.com/aspnet/core/fundamentals/http-requests?view=aspnetcore-8.0&WT.mc_id=%3Fwt.mc_id%3DMVP_452430#streaming)
- [Microsoft: Server-Sent Events (SSE)](https://learn.microsoft.com/azure/application-gateway/for-containers/server-sent-events?tabs=server-sent-events-gateway-api&WT.mc_id=%3Fwt.mc_id%3DMVP_452430)
- [Microsoft: CORS в ASP.NET Core](https://learn.microsoft.com/aspnet/core/security/cors?view=aspnetcore-8.0&WT.mc_id=%3Fwt.mc_id%3DMVP_452430)
- [Python requests: Потоковые запросы](https://requests.readthedocs.io/en/latest/user/advanced/#streaming-requests)

### Что дальше?

- Попробуйте создавать более продвинутые MCP-инструменты, использующие потоковую передачу для аналитики в реальном времени, чатов или совместного редактирования.
- Исследуйте интеграцию MCP streaming с фронтенд-фреймворками (React, Vue и т.д.) для обновления UI в реальном времени.
- Далее: [Использование AI Toolkit для VSCode](../07-aitk/README.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:
Этот документ был переведен с использованием сервиса машинного перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия по обеспечению точности, имейте в виду, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на его исходном языке следует считать авторитетным источником. Для получения критически важной информации рекомендуется обратиться к профессиональному человеческому переводу. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования этого перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->